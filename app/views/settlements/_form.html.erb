<%= form_with(model: settlement) do |form| %>
  <% if settlement.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(settlement.errors.count, "error") %> prohibited this settlement from being saved:</h2>

      <ul>
        <% settlement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :type, style: "display: block" %>
    <%= form.text_area :type %>
  </div>

  <div>
    <%= form.label :external_reference, style: "display: block" %>
    <%= form.text_area :external_reference %>
  </div>

  <div>
    <%= form.label :external_amount, style: "display: block" %>
    <%= form.number_field :external_amount %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
